Start containers with:

  docker-compose -p dssim -f docker-compose.yml up -d

Re-run the same command to start the performance test again.

Stop containers with:

  docker-compose -p dssim -f docker-compose.yml stop

